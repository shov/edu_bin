import React from 'react'
import {View, Image, TouchableHighlight} from 'react-native'

export default function ImageViewScreen({screenSwitcher}: {screenSwitcher: (name: string) => void}) {
    return (
        <View></View>
    )
}